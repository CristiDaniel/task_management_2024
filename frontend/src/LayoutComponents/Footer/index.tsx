import styles from './index.module.css'
const Footer = () => {
    return <footer className={styles.footer}>
        <span className={styles.copywright}>Task Management Project</span>
    </footer>
}

export default Footer;